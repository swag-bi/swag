package swag.analysis_graphs.execution_engine.analysis_situations;

public enum AggregationFunction {
  AVG, COUNT, SUM, MIN, MAX, AVG_DISTINCT, COUNT_DISTINCT, SUM_DISTINCT, MIN_DISTINCT, MAX_DISTINCT
}
